<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="csMapper">
	<!-- 게시판 글 작성 -->
	<insert id="insert">
	    INSERT INTO CUSTOMERSERVICE(cs_memid 
	   					       , cs_content)
	                  VALUES(   #{cs_memid}
	                 		   , #{cs_content})

    </insert>
	
</mapper>

